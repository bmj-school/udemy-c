{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "my gcc build",
            "type": "shell",
            "command": "gcc",
            "args": ["-g", "${fileDirname}/${fileBasename}", "-o", "${fileDirname}/a.out", "-v"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}